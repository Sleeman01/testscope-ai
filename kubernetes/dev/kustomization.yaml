apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: dev
resources:
  - ../base
  - hpa.yaml
patches:
  - path: configmap-patch.yaml
  - path: ingress-patch.yaml
replicas:
  - name: api
    count: 1
  - name: worker
    count: 1
