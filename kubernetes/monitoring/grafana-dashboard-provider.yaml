apiVersion: v1
kind: ConfigMap
metadata: { name: grafana-dashboard-provider, namespace: monitoring }
data:
  dashboards.yaml: |
    apiVersion: 1
    providers:
      - name: testscope
        folder: ""
        type: file
        options:
          path: /etc/grafana/dashboards
